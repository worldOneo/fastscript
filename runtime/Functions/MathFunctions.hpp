#include "Function.hpp"

namespace fastscript::runtime {
    function_with_name(Add);
    function_with_name(Divide);
    function_with_name(Subtract);
    function_with_name(Multiply);
    function_with_name(LogicOR);
    function_with_name(LogicXOR);
    function_with_name(LogicAND);
    function_with_name(AsBoolean);
}